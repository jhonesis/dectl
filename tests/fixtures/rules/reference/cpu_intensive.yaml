# .dec/rules/profiles/cpu_intensive.yaml — schema_version 2
# Generated by: rules/partition_rules.py from rules/rules.yaml. DO NOT EDIT BY HAND
# (readable cache — the source is the embedded RULES_YAML; `dectl rules resync` overwrites).

version: 2
profile_flag: cpu_intensive
source_rule_ids:
- '6.4'
rules:
- id: '6.4'
  section: Async Programming
  severity: MUST
  primary_flag: cpu_intensive
  requires:
  - flag: cpu_intensive
    equals: true
  condition: the task is CPU-bound (image processing, heavy computation, AI)
  action: use multiprocessing/threads instead of async
  why: the event loop does not speed up CPU work
  exception: null
  tags:
  - async
  - cpu
  - multiprocessing
